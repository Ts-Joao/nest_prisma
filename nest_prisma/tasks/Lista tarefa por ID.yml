info:
  name: Lista tarefa por ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{api_url}}/tasks/100"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
